# 🚀 نماء كارد — دليل التشغيل خطوة بخطوة

اتبع الخطوات بالترتيب. كل خطوة ليها مكانها.

---

## 📦 الخطوة 0: قبل ما تبدأ

انت محتاج:
- حساب على [Vercel](https://vercel.com) (مجاني)
- حساب على [Supabase](https://supabase.com) (مجاني)
- مفاتيح كاشير (نفس بتوع فرصة)
- حساب Bitnob (تطلبه بإيميل — بياخد وقت)

---

## 🗄️ الخطوة 1: Supabase (قاعدة البيانات)

1. ادخل [supabase.com](https://supabase.com) → **New Project**
2. اختار اسم وباسورد لقاعدة البيانات (احفظهم)
3. استنى دقيقتين لحد ما المشروع يجهز
4. من القايمة الجنب → اضغط **SQL Editor**
5. اضغط **+ New query**
6. افتح ملف `supabase/SUPABASE_SETUP.sql` من المشروع ده
7. **⚠️ غيّر الإيميل** في آخر الملف لإيميلك الحقيقي
8. انسخ الملف كله والصقه في SQL Editor
9. اضغط **RUN** (أو Ctrl+Enter)
10. المفروض تشوف "Success"

### دلوقتي اعمل حساب الأدمن:
11. من القايمة الجنب → **Authentication** → **Users**
12. اضغط **Add user** → **Create new user**
13. اكتب **نفس الإيميل** اللي حطيته في الخطوة 7 + باسورد
14. اضغط **Create user**

### خد المفاتيح (هتحتاجهم في Vercel):
15. من القايمة الجنب → **Project Settings** (الترس) → **API**
16. انسخ واحفظ:
    - **Project URL** (مثال: https://abcd.supabase.co)
    - **anon public** key
    - **service_role** key (ده سري — متشاركهوش)

---

## 🔑 الخطوة 2: ولّد مفتاح التشفير

افتح موقع [generate-random.org/encryption-key-generator](https://generate-random.org/encryption-key-generator?count=1&bytes=32&type=hex)

أو لو عندك Node على الكمبيوتر، افتح Terminal واكتب:
```
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

هيطلعلك كود طويل (64 حرف). **احفظه** — ده `CARD_ENC_KEY`.

---

## ▲ الخطوة 3: Vercel (رفع الموقع)

### أ. ارفع المشروع
1. ادخل [vercel.com](https://vercel.com) → **Add New** → **Project**
2. لو المشروع على GitHub: اربطه
   - أو: اضغط **Deploy** واسحب فولدر المشروع (بعد ما تفك الـ ZIP)
3. **متضغطش Deploy لسه!** لازم تحط المفاتيح الأول ↓

### ب. حط المفاتيح (Environment Variables)
في صفحة الإعداد، افتح **Environment Variables** وحط دول واحد واحد:

| الاسم | القيمة |
|-------|--------|
| `VITE_SUPABASE_URL` | Project URL من Supabase |
| `VITE_SUPABASE_ANON_KEY` | anon key من Supabase |
| `SUPABASE_URL` | نفس Project URL |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role key من Supabase |
| `KASHIER_MERCHANT_ID` | من كاشير |
| `KASHIER_API_KEY` | من كاشير |
| `KASHIER_MODE` | `test` |
| `KASHIER_ALLOWED_METHODS` | `card,wallet` |
| `BITNOB_BASE_URL` | `https://sandboxapi.bitnob.co` |
| `BITNOB_SECRET_KEY` | من Bitnob |
| `CARD_ENC_KEY` | المفتاح من الخطوة 2 |
| `APP_BASE_URL` | `https://اسم-مشروعك.vercel.app` |
| `CRON_SECRET` | أي كلمة سر تختارها |
| `N8N_NOTIFY_WEBHOOK` | (سيبه فاضي مؤقتًا) |

> **ملاحظة عن APP_BASE_URL:** أول مرة مش هتعرف الرابط. سيبه فاضي أو حط أي حاجة، وبعد أول Deploy هيديك الرابط، ارجع عدّله واعمل Redeploy.

### ج. اضغط Deploy
استنى لحد ما يخلص. المفروض يشتغل من غير أخطاء دلوقتي (عكس المرة اللي فاتت — المشكلة كانت الكرون واتصلحت).

---

## 🔗 الخطوة 4: اربط الويبهوكس

### كاشير:
- ادخل لوحة كاشير → Webhooks / Integrations
- حط: `https://اسم-مشروعك.vercel.app/api/webhooks/kashier`

### Bitnob:
- ادخل لوحة Bitnob → Webhooks
- حط: `https://اسم-مشروعك.vercel.app/api/webhooks/bitnob`

---

## 🧪 الخطوة 5: الاختبار الحاسم (مهم جدًا!)

**قبل ما تطلق لأي عميل:**

1. سيب `KASHIER_MODE=test` و `BITNOB_BASE_URL` على sandbox
2. ادخل موقعك، اعمل طلب كامل
3. **جرّب الكارت اللي طلع فعليًا على اشتراك قناتك**
4. لو اتقبل ✅ → غيّر `KASHIER_MODE=live` و `BITNOB_BASE_URL=https://api.bitnob.co` واعمل Redeploy
5. لو اترفض ❌ → كلّم Bitnob واطلب BIN تاني

---

## 🎛️ الخطوة 6: لوحة التحكم

- ادخل: `https://اسم-مشروعك.vercel.app/admin`
- سجّل دخول بالإيميل والباسورد بتوع الخطوة 1
- من هنا تتحكم في الباقات، تشوف الطلبات، وتدير الكروت

---

## ❓ لو حصلت مشكلة

- **Build فشل؟** اتأكد إنك رافع آخر نسخة (فيها تعديل الكرون)
- **مش بيدخل الأدمن؟** اتأكد إنك عملت User في Authentication بنفس إيميل جدول admins
- **الكارت مش بيتصدر؟** اتأكد من مفاتيح Bitnob وإنه فعّل حسابك

بالتوفيق! 🎉
